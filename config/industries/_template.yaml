
# ---------------------------------------------------------------------------
# Outreach (M1.4a) — which qualification bands Sales drafts first-touch email for.
# [sql] matches agent-contracts.md §2/§3. mql is nurture (Phase 4); add it here
# only as a deliberate decision.
# ---------------------------------------------------------------------------
outreach:
  draft_bands: [sql]
